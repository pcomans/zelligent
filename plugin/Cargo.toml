[package]
name = "zelligent-plugin"
version = "0.0.0-dev"
edition = "2021"

[[bin]]
name = "zelligent-plugin"
path = "src/main.rs"

[dependencies]
zellij-tile = "0.43"

# Dev-deps are only used by `cargo test --target <host>`, never compiled for wasm.
[dev-dependencies]
insta = "1.46"
